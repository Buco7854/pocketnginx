# lua-nginx-module + LuaJIT, compiled into this image against the exact
# nginx it ships. Symlinked from modules-enabled with a 50- prefix (after
# NDK). Required by the CrowdSec bouncer (activated by CROWDSEC_LAPI_KEY).
load_module /usr/lib/nginx/modules/ngx_http_lua_module.so;
